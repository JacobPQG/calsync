// ─── StatsPanel (MVVM entry point) ────────────────────────────────────────────
//   • useStatsPanelVM.ts   — logic (tab flags, standings/recent/challenges).
//   • StatsPanel.view.tsx  — pure view (JSX + STYLE). Reshape freely here.
export { StatsPanel } from './StatsPanel.view'

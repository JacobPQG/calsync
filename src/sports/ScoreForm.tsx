// ─── ScoreForm (MVVM entry point) ─────────────────────────────────────────────
//   • useScoreFormVM.ts   — logic (draft teams, validation, save/remove).
//   • ScoreForm.view.tsx  — pure view (JSX + STYLE). Reshape freely here.
export { ScoreForm } from './ScoreForm.view'

// ─── DayView (MVVM entry point) ───────────────────────────────────────────────
//   • useDayViewVM.ts    — logic (timeline math, day state, detail derivation).
//   • DayView.view.tsx   — pure view (JSX + STYLE block). Reshape freely here.
// This file re-exports the view so import paths stay stable.

export { DayView } from './DayView.view'

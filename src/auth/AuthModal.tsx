// ─── AuthModal (MVVM entry point) ─────────────────────────────────────────────
//   • useAuthModalVM.ts   — logic (fields, sign-in/up orchestration).
//   • AuthModal.view.tsx  — pure view (JSX + STYLE). Reshape freely here.
export { AuthModal } from './AuthModal.view'

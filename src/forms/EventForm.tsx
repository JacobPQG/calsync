// ─── EventForm (MVVM entry point) ─────────────────────────────────────────────
//   • useEventFormVM.ts   — logic (form state, validation, submit payload).
//   • EventForm.view.tsx  — pure view (JSX + STYLE + option lists). Reshape here.
// This file re-exports the view so import paths stay stable.

export { EventForm } from './EventForm.view'

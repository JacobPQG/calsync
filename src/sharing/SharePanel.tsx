// ─── SharePanel (MVVM entry point) ────────────────────────────────────────────
//   • useSharePanelVM.ts   — logic (load grants, toggle shares).
//   • SharePanel.view.tsx  — pure view (JSX + STYLE). Reshape freely here.
export { SharePanel } from './SharePanel.view'
